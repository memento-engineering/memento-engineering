.DS_Store
.dart_tool/

# the_grid live-arm scratch (the station cuts per-bead worktrees here when THIS
# checkout is the substation root). Never tracked — throwaway per-run state.
.grid/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
